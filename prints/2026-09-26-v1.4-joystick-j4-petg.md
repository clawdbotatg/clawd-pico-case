# v1.4 J4 joystick — gray PETG

Austin approves the 7.4 mm half-ball J4 ("that's what we want") and asks for
the joystick only. He gave no colour, so it uses gray PETG from bay 3, the
same as the last caps. It fits the existing v1.3 lid (same 8 mm hole) as
well as the v1.4 lid.

Source 5a98998, `stl/v1.4/joystick.stl`, SHA256
`657f9e9f6fc75517705cd6a755b3588075f9c89980d2ca11b1daf59f1fc5d626`.
10.4 × 10.4 × 5.1 mm. PETG, 0.16 mm, 4 walls, no supports, no raft,
flange down.

Sent to the HTTP inbox as `20260926-112324-joystick` with a GO (queue after
any running job). Status: new. Print start unconfirmed.

Check: the press-in click is easy to hit, the ball passes up through the lid
hole, and all four directions tilt freely.

## Result — FAILED, cause found

The print Mac printed 6× J4 + 12× v1.0 buttons in grey PETG (A3) on Austin's
direct request, finishing at 11:47. Austin: the J4 socket "doesn't work, it's
messy"; the J2 socket was much better (photos in `v1.4-photos/`).

Cause: a CAD error by Claude. J4 added a solid 7.4 mm sphere centred at
z 8.5. Its bottom reaches z 4.8, inside the stick socket (z 3.4–6.4), so the
union filled the top 1.5 mm of the 2 mm square. The socket was about 1.4 mm
deep instead of 3.0 (the void was 28.69 mm³ instead of 34.78). The check
"unchanged below the ball" started at z 4.8 and could not see this. The print
settings matched the good J2 print (grey PETG, A3, 4 walls). Fix in the next
commit: re-cut J2's exact socket after adding the ball, and check the socket
void against J2. The six printed J4 caps are scrap.
